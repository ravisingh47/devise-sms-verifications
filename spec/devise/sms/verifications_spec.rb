require 'spec_helper'

describe Devise::Sms::Verifications do
  it 'has a version number' do
    expect(Devise::Sms::Verifications::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
